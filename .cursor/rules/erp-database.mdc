---
description: Database is out of scope for cursor-customer-portal
alwaysApply: true
---

# DATABASE — out of scope

This folder does not own the database.

Never add MySQL, Sequelize, SQL, connection pools, `.env` DB credentials, or migrations here.

Schema, tenant isolation, and data changes belong to **Agent API** (`cursor-node-api`).
