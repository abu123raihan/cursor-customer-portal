---
description: Angular ERP admin and website standards
globs:
  - "src/**/*.ts"
  - "src/**/*.html"
  - "src/**/*.scss"
alwaysApply: false
---

# FRONTEND

Use Angular 22 (zoneless, no `zone.js`) for cursor-admin-ui and cursor-customer-portal.

Requires Node.js `^22.22.3 || ^24.15.0 || >=26.0.0` and TypeScript 6.0.x.

Angular applications consume ERP-API.

Never connect Angular directly to MySQL.

Never duplicate server-side business logic in Angular.

Use typed API models/services.

Do not invent endpoints.

Respect API contracts.

Implement loading, empty, error and validation states.

Admin UI must enforce permission-aware visibility but API authorization remains the security boundary.

Website must be responsive and mobile-friendly.
